# m5stack-core-mp135-display

## How to run

**M5 Stack:**
```bash
pip install -e ".[deployment]"
python -m application.main
```

**Simulator (Dev-PC):**
```bash
pip install -e ".[development]"
python -m application.simulator
```